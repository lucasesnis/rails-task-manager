<h1><%= @task.title %></h1>
<p><%=  @task.details %></p>
<p><%= @task.completed ? " ☑️ Task is completed" : " 🔴 Task is not completed yet" %></p>

<%= link_to("Edit", edit_task_path(@task)) %> <br>
<%= link_to("Back to tasks", tasks_path) %> <br>
<%= link_to('Destroy', task_path(@task), method: :delete) %>
